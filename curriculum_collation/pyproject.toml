[project]
name = "curriculum_collation"
authors = [
  { name = "DappoF", email = "admin@example.com" }
]
description = "ERPNext module for curriculum collation and instructional planning"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
