[project]
name = "education"
authors = [
    { name = "DappoF", email = "devnull@example.com" }
]
description = "Education custom app"
readme = "README.md"
requires-python = ">=3.10"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "education"

[tool.bench.frappe]
app_name = "education"
